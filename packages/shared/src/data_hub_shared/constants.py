"""Shared constants for the Data Hub packages.

Instrument display names and IDs now live in the web app's ``instruments``
table (and the Lambda resolves processors by ``instrument_type``). This
module is reserved for any remaining cross-package constants.
"""
